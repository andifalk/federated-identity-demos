spring:
  application:
    name: rfc-9068-resource-server
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://localhost:9500/oauth2/jwks
logging:
  level:
    org:
      springframework:
        security:
          oauth2: trace

server:
  port: 9090
